# ==============================================================================
# SPIRE / SPIFFE Keyless Authentication Lab Configuration
# ==============================================================================

# Node attestation join token (deterministic for local lab)
JOIN_TOKEN=demo-lab-join-token

# Trust domain
TRUST_DOMAIN=demo.local

# Workload socket path
SPIFFE_ENDPOINT_SOCKET=unix:///run/spire/sockets/agent.sock
